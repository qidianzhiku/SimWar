# M29 MAIN-pull source-backed consumption

M29 turns the M25 public-source epoch, M27 exact Hangzhou regional-transfer candidate, and M28 dual-epoch operation pack into one explicit MAIN binding request. The State A → State B transition is:

`SOURCE_PACK_WITHOUT_MAIN_PULL_BINDING` → `SOURCE_BACKED_SHANGHAI_REGIONAL_ENTERPRISE_JOURNEY_MAINLINE_BOUND_WITH_LIMITS`

The support pack defines source-backed Shanghai-to-Hangzhou regional consumption and a public-safe enterprise candidate, plus Teacher, Student, Admin, and Enterprise journey projections. Each consumer remains exact-version bound, `LIMITED`, `NOT_PROVEN` for calibration, and `LOOKAHEAD_READY` rather than formally admitted.

MAIN owns the shared regional-transfer contract, OpenAPI, Role BFF, formal writer, registry, Product PR, merge, and H3. SH creates only the candidate pack, fixtures, limits, and `SH-M29-MAIN-PULL-BINDING-REQUEST`. The pack deliberately records route/service/BFF/browser proof as `NOT_PROVEN`; no SH-owned route or second consumer is added.

No official Truth, Settlement, Score, Rank, ParameterSet, Provider, database, Pilot, Production, Human Validation, or automatic successor state is written. Public rights, exact locators, expiry, and qualification must be rechecked by MAIN before any formal binding.
