# R7 BFF Endpoint No-Go Register

Stop the next implementation gate if any of these appear:

- student private visibility or protected truth exposure;
- cross-tenant or cross-team scope;
- direct internal settlement-route access;
- official result, `SettlementResult`, `state_true`, or ParameterSet write;
- Replay execution or overwrite of an official result;
- runtime activation, service registration, IO, schema, or database drift;
- AI or Plugin runtime scope entering the endpoint package.

Current package status: contract-only, no route, no IO, no runtime activation.
