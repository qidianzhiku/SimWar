# SimWar M2-P5 decision-learning cross-round governance closure

Date: 2026-08-23

Mission: `SIMWAR-SH-M2-P5-DECISION-LEARNING-CROSSROUND-V5.22-20260823`

Mission run: `SIMWAR-SH-M2-P5-20260823-DF70AC1765324884971399A09AF7806F`

## Closure decision

`PASS_WITH_LIMITS` for the bounded M2-P5 decision-learning cross-round scope.

This is the single docs-only Governance Closure for Product PR #442. The
Product PR was ordinarily merged only after its exact head passed the required
GitHub quality, browser-smoke, JavaScript/TypeScript analysis and CodeQL checks,
and after all review conversations were resolved. This closure records the
read-only projection and the fresh detached post-merge evidence; it does not
create a second Product, Decision, Settlement, Round, Publication, Learning,
EnterpriseState, provider, model, PostgreSQL/RLS, W6, Human Validation, Pilot,
Production, release, or automatic-successor authority.

The uploaded V5.22 Prompt is the mission target and execution boundary. The
Macro Development Plan is supporting planning context only. The separately
expected allowlist/reference pack was not present in the attached-download
set, so `MISSION_AUTHORITY_DIGEST_STATUS` remains
`PENDING_ALLOWLIST_PACK_IDENTITY` and is not represented as a successful
allowlist verification.

## Exact Product and source readback

- Product PR: [#442](https://github.com/qidianzhiku/SimWar/pull/442)
- Product source branch:
  `codex/m2-p5-v522-decision-learning-20260823`
- Product PR base: `6a5e4fcc563706de2acab81945588fbe8869213f`
- Product PR head before merge: `e6134ce9a28d2546df514f4b2a728b2f524e1204`
- Product merge commit: `2fe9650ee7299cc5c506a75741bdeb97eda3e4b4`
- Product merge tree: `bbc0bb1d8fb8244219b3b03a0bd18d975a449d8d`
- Fresh detached post-merge worktree:
  `D:\codex\SimWar-m2p5-post-merge-20260823`
- Detached post-merge checkout:
  `2fe9650ee7299cc5c506a75741bdeb97eda3e4b4`
- Product PR changed-file count: 38; the exact manifest is retained in
  `docs/evidence/m2p5-decision-learning-crossround/M2P5_EVIDENCE_INDEX.md`.
- Governance PR number and its merge SHA are intentionally bound by the
  final external readback after the ordinary Governance merge; this document
  cannot truthfully predict the SHA of the commit that contains itself.

## Delivered Product scope

- A single exact-scope M2-P5 BFF composition reads the existing Course, Run,
  Round, Team, W3 consequence, confirmed-only D4 learning report, Project
  Library reference, and W4 Closing-to-Opening lineage.
- Teacher and the authorized Student receive the same bounded cross-round
  projection through their respective persona-safe paths; Student output is
  restricted to the assigned team and does not expose `state_true`, replay
  internals, teacher-only evidence, or another team.
- Missing publication, confirmation, learning, project, W4 lineage, or
  next-round prerequisites remain explicitly blocked with named reasons. No
  latest/current/default/fallback reference is inferred.
- The existing Round continuation authority remains the only action/write
  path. The new boundary is read-only and does not persist learning, state,
  settlement, publication, decisions, or enterprise state.
- Counterfactual information remains explicitly non-official and non-persistent.
- The real two-round browser fixture exercises the live BFF path with mocks and
  retries disabled, but the fixture seeds synthetic `SettlementResult` data
  including `state_true`, score and rank, and pre-creates an open second round
  before the browser starts. It proves the live projection, visibility and
  authority boundaries over that seeded state; it does not prove the workflow
  that generated settlement/round truth and is not Human Validation.

## Authority and safety boundaries

The existing Course, Run, Round, Team, canonical Decision, W3 consequence, D3
confirmation, D4 report, Project Library, W4 EnterpriseState, Publication and
persona-safe BFF paths remain authoritative for their respective domains. The
M2-P5 layer only composes exact read projections and presentation cards.

The Product PR did not change settlement logic, `SettlementResult`, canonical
decision selection, replay truth hash inputs, formal EnterpriseState writers,
round continuation writers, publication visibility, provider/model activation,
or the JSON-versus-PostgreSQL runtime boundary. No second Truth or Settlement
authority was introduced.

## Validation

### Product PR exact-head checks

At Product head `e6134ce9a28d2546df514f4b2a728b2f524e1204`:

- `quality`: `PASS`;
- `browser-smoke`: `PASS`;
- `Analyze JavaScript and TypeScript`: `PASS`;
- CodeQL: `PASS`;
- PR state immediately before merge: `OPEN`, non-Draft, `MERGEABLE`, `CLEAN`;
- four review threads were resolved, including the D2 round-binding finding,
  the Teacher EvidenceWorkbench scope finding, the M2-P5 authentication
  status finding, and the CodeQL test-data finding;
- ordinary merge used no admin bypass, auto-merge, force push, or protected
  branch mutation.

### Product and detached post-merge validation

- Product local targeted typecheck: `PASS`.
- Product local lint: `PASS`.
- Product local M2-P5 and remediation integration tests: `PASS`.
- Product local contract gate: `PASS`, 33 files / 79 tests.
- Detached post-merge `npm ci`: `PASS`; inherited dependency audit output was
  2 low and 7 high advisories, with no dependency or lockfile change.
- Detached post-merge contract gate: `PASS`, 33 files / 79 tests.
- Detached post-merge build prerequisites and UI build: `PASS`.
- Detached post-merge real-BFF browser journey:
  `PASS`, 1/1 test, isolated ports 3320-3323, mocks=0, retries=0.
- The default browser attempt on port 3100 was `ENV BLOCKED` with
  `EACCES`; no unrelated process was stopped, and the same journey passed on
  isolated ports.
- The local concurrent full `npm test` run had inherited environment timing
  failures in unrelated timeout-sensitive tests; the required remote `quality`
  check passed on the exact Product head. The local aggregate is not reported
  as a full pass.

The detailed receipts are:

- `docs/evidence/m2p5-decision-learning-crossround/M2P5_EVIDENCE_INDEX.md`
- `docs/evidence/m2p5-decision-learning-crossround/PRODUCT_EXACT_HEAD_VALIDATION_RECEIPT.md`
- `docs/evidence/m2p5-decision-learning-crossround/POST_MERGE_DETACHED_RECEIPT.md`
- External mission receipts under
  `C:\Temp\simwar-m2p5-SIMWAR-SH-M2-P5-20260823-DF70AC1765324884971399A09AF7806F\`.

## Known limits and explicit non-claims

- `PASS_WITH_LIMITS` is not a full WCAG or accessibility PASS. No complete
  Human Validation was performed, and automated browser evidence does not
  establish teaching effectiveness or usability.
- The runtime remains JSON-internal. PostgreSQL application runtime, RLS
  activation, migration rollout, provider activation and real-model activation
  are not claimed.
- W6, Human Validation, Pilot, Production, release approval and an automatic
  successor are outside this mission and remain unauthorized.
- The V5.22 allowlist/reference pack identity is pending because it was not
  found in the supplied attachment set; current source, exact Git state and
  executable checks are the authoritative evidence used here.
- npm audit reported inherited advisories; this mission made no dependency
  change and does not convert that output into a security pass.
- The CodeQL file-data report was reviewed as a test-only false positive: the
  test starts a local `127.0.0.1` server and sends deterministic digest/reference
  metadata, not file contents or secrets, to an external destination. The PR
  review thread was answered and resolved; the underlying CodeQL check remains
  recorded as passed.

## Planning pointer, lock release and rollback

The mutable planning pointers are reconciled to the M2-P5 Product merge and
the exact Product evidence in this docs-only PR. Their governance merge fields
remain `PENDING_UNTIL_FINAL_READBACK` until the external final readback binds
the actual Governance PR number, merge SHA and current `origin/master`.

The M2-P5 Product and Governance closure locks are released only after the
ordinary Governance merge and exact final master readback. No next candidate is
started automatically; the backlog remains pending explicit Owner direction.

Product rollback is the normal reviewed revert of Product PR #442. This
docs-only Governance Closure does not mutate runtime Course, Run, Round,
Decision, Settlement, Replay, Publication, EnterpriseState, provider,
deployment or Production state.
