# SimWar W5 Governed Model & Shanghai Convergence Governance Closure

## Document control

| Field | Value |
| --- | --- |
| Document ID | `W5-GOV-00` |
| Status | `GOVERNANCE_CLOSURE_CANDIDATE_PASS_WITH_LIMITS` |
| Date | `2026-08-20` |
| Repository | `qidianzhiku/SimWar` |
| Default branch | `master` |
| W5 execution base | `2304f79b70d68f62a504073ae90232e1f52719cd` |
| Product PR | [#402](https://github.com/qidianzhiku/SimWar/pull/402) |
| Product head | `6dc18fc80f224eef07cf0b7d226abb57f475f116` |
| Product merge | `6f5881240247cff6da8fdf1d3d7e814afe1840b5` |
| Governance PR | PENDING UNTIL THIS CANDIDATE IS PUBLISHED |
| Governance merge | PENDING UNTIL GOVERNANCE MERGES |
| Current master at closure preparation | `6f5881240247cff6da8fdf1d3d7e814afe1840b5` |
| Governance reclosure count | `0` |
| `automatic_next_start` | `false` |

This is the one W5 docs-only Governance Closure. It records the bounded W5
product outcome after Product PR #402 and its fresh detached validation. It
does not rewrite the predecessor record, create a second model authority, or
authorize a successor wave.

## Predecessor disposition

The W4 predecessor-integrity bridge completed before W5 began:

| Item | Identity |
| --- | --- |
| W4 remediation Product PR | [#399](https://github.com/qidianzhiku/SimWar/pull/399) |
| W4 remediation Product head | `db8d97ac92d34479b89751a2506bf9897f4fe1aa` |
| W4 remediation merge | `b95aaaf9b93a99261acc28780b5fbc4d92d8a79a` |
| W4 Governance Delta PR | [#400](https://github.com/qidianzhiku/SimWar/pull/400) |
| W4 Governance Delta merge | `2304f79b70d68f62a504073ae90232e1f52719cd` |
| Issue #398 | [CLOSED](https://github.com/qidianzhiku/SimWar/issues/398) |
| Issue #398 closure evidence | [closure comment](https://github.com/qidianzhiku/SimWar/issues/398#issuecomment-5355799546) |

Historical W4 Product/Governance records remain immutable. The W4 delta's
known limits remain in force; this W5 closure does not convert them into a
global accessibility, security, PostgreSQL/RLS, Human Validation, Pilot, or
Production pass.

## W5 primary outcome

`W5_GOVERNED_MODEL_SHANGHAI_CONVERGENCE_CLOSED_LOOP` is implemented as one
governance-plane capability:

Teacher can author, validate, freeze, and exactly bind a Course, Scenario,
Parameter, and Model configuration for Shanghai Standard/Advanced views. The
same Simulation Core evaluates the realized projection. Approved model views
may produce WANT candidates and CAN constraints; they do not become a second
Truth, Settlement, Enterprise State, Score, or Rank writer.

The merged Product PR contains 21 W5 files across the bounded frontend,
shared-contract, OpenAPI/schema, API service/route, Simulation Core adapter,
and focused test surfaces. Its final Product diff against the product base
contains only the W5 implementation; governance is not mixed into that PR.

## M-RB0 — current fact map

The W5 source map is recorded as follows:

| Area | Classification | W5 disposition |
| --- | --- | --- |
| Course/Run/Round exact references | CURRENT | Resolved through the existing repository facade and exact run/round route checks. |
| Scenario and Parameter references | CURRENT | Bound by explicit IDs, versions, and content digests; no implicit latest. |
| ModelVersion and parameter descriptors | CURRENT | Exposed by the governed model service and teacher projection. |
| Teacher Scenario Studio | CURRENT | Author → validate → freeze → exact bind lifecycle is implemented and browser-tested. |
| Capacity, workforce, quality, and eligibility | CURRENT | CAN constraints are derived from the Simulation Core evaluation path. |
| Simulation Core realized plane | CURRENT | REALIZED is produced by the existing core evaluator. |
| Ideal Point/Lancaster and BLP/RCNL labels | DECLARED CANDIDATE/SHADOW OWNERSHIP | The current W5 implementation does not invoke these engines; WANT is a synthetic heuristic and cannot write formal truth. |
| Huff/spatial choice | SHADOW | Candidate ownership is explicit; it is not a formal result authority. |
| System Dynamics | SHADOW | Lag/stock/flow output is non-official and cannot overwrite official results. |
| Shanghai reality calibration | LIMIT/RESEARCH | Synthetic and assumption-labelled; reality calibration is not proven. |
| Human validation | LIMIT | A/B were not performed; only HV-B readiness may be stated. |

The named W5 Plan and Upload Allowlist/Reference Extract were not available in
the repository or supplied attachment set during the fresh W5 context read.
That document-availability fact is retained as a known limit; the closure is
based on current source, tests, exact Git identities, and the signed W5 scope.

## M-RB1/RB2 — model, version, ownership, and canonical gate status

This section records the W5 implementation inventory; it does not replace the
canonical model-governance gate records. The current dispositions remain
`M-RB1=PARTIAL`, `M-RB2=NOT_RUN` (including the required
Golden/Differential/Replay reproduction), and `M-RB3=NOT_PASSED`.
Downstream model activation remains unauthorized by this closure. No statement
below should be read as a formal M-RB pass or as evidence that the referenced
BLP, RCNL, Lancaster, or ideal-point engines have been implemented or
validated.

The bounded W5 declared model identity is:

- `ModelVersion`: `eldercare_w5_governed_v1@1.0.0`;
- model family: `eldercare_core_model_v1`;
- engine reference: `eldercare_core_model_v1@1.0.0`;
- `no_implicit_latest: true`;
- deterministic fallback: `PLANE_OFF → CORE_ELDERCARE_V1`;
- fallback continues the official path and does not create a second runtime.

The seven explicit feature ownership records are:

| Feature | Primary producer | Visibility / meaning |
| --- | --- | --- |
| `preference_fit` | `ideal_point_lancaster` | Declared candidate ownership label; not evidence that an ideal-point/Lancaster engine was invoked or validated. |
| `latent_demand` | `blp_rcnl_choice_candidate` | Declared candidate ownership label; not evidence that a BLP/RCNL engine was invoked or validated. |
| `spatial_access` | `huff_spatial_choice_candidate` | Shadow spatial-access candidate. |
| `service_capacity` | `simulation_core_operations` | Approved capacity constraint. |
| `workforce_feasibility` | `simulation_core_operations` | Approved staffing constraint. |
| `realized_outcome` | `simulation_core` | Internal official-core projection, with no formal-result write. |
| `sd_lag_candidate` | `system_dynamics_shadow` | Shadow lag/stock/flow hypothesis. |

The teacher surface exposes 12 parameter descriptors. Parameters not mapped to
an approved consumer remain draft-only and block validation rather than being
silently promoted into runtime truth.

## M-RB3 — convergence and authority boundaries

The W5 convergence projection has three deliberately separate planes:

- `WANT`: synthetic heuristic value derived from the existing core demand index
  and `aging_rate`; the BLP/RCNL/Lancaster/ideal-point labels are ownership
  metadata only, no such engine is invoked by the current implementation, and
  `official=false`.
- `CAN`: capacity/workforce/quality/eligibility constraints;
  `official=false`.
- `REALIZED`: metrics from `Simulation Core`; the projection reports
  `authority=SIMULATION_CORE` and `official=true`, while
  `writes_formal_result=false`.

Standard and Advanced are two views over the same bound input and core path.
The focused integration test verified equal `replay_relevant_digest` values
for both profiles. The browser journey verified the Standard path through the
Teacher Studio. Plane-OFF fallback retains the core path and is covered by the
service tests.

W5 does not alter settlement logic, `SettlementResult`, replay truth hash
inputs, canonical decision selection, W4 persistent state, or Enterprise State
authority.

## Teacher Studio and Shanghai outcome

The Teacher Studio supports the bounded lifecycle:

1. create a synthetic/assumption-labelled draft;
2. validate mapped parameters;
3. freeze the draft;
4. bind the exact run, round, scenario reference, parameter reference, seed,
   and content digests;
5. evaluate Standard or Advanced through the same core authority.

The real browser journey passed on the Product head and again in the fresh
detached merge checkout. The Shanghai label is intentionally honest: the
current evidence is synthetic/assumption-labelled and does not claim calibrated
Shanghai reality, production economics, or human-validated model fit.

## Shadow, differential, replay, and fallback

- System Dynamics is `non_official` and `overwrites_official_result=false`.
- Convergence replay is `NON_OFFICIAL`; it cannot write official results.
- A bound projection reports `exact_identity=READY` and includes the exact
  binding digest, versioned parameter reference, versioned scenario reference,
  and seed.
- Evaluation now fails closed unless the draft is `BOUND` and the supplied
  run/round exactly match the stored binding.
- `PLANE_OFF` is deterministic and explicitly continues the core official
  path.

## Security proof and student visibility

The W5 security context records actor, tenant, course, run, round, team, role,
and activity dimensions with enforcement point, negative-test description,
and evidence references. The route/service boundary enforces:

- tenant and course ownership;
- exact run and round existence;
- exact bound run/round identity before evaluation;
- student team enrollment before returning the student projection;
- teacher-only lifecycle mutation;
- cross-tenant and unknown-scope failure closed.

The focused W5 tests cover unbound evaluation, wrong run/round, unenrolled
student access, and cross-tenant access. The student projection is
`ROLE_SAFE_STUDENT` and does not expose teacher `parameter_values` or bound
`content_digest` values.

Create, validate, freeze, and bind are persisted by the JSON runtime together
with server-side audit entries. Drafts remain governance-plane records and are
not canonical settlement records.

## Exact validation evidence

### Product PR exact head

PR #402 was merged only after exact-head readback showed:

- `quality`: PASS;
- `browser-smoke`: PASS — 4 PR4 tests passed; core browser 117 passed / 11
  skipped;
- `Analyze JavaScript and TypeScript`: PASS;
- CodeQL: PASS;
- PR state: merged normally, without force/admin bypass or auto-merge.

### Fresh detached merge validation

Fresh detached worktree:
`D:\codex\worktrees\simwar-w5-post-merge-6f58812`

Exact detached `HEAD`:
`6f5881240247cff6da8fdf1d3d7e814afe1840b5`

The worktree was clean before and after validation. After the declared
`npm ci` and monorepo build prerequisites, the detached checkout passed:

- `npm test`: 218 test files / 1355 tests;
- `npm run build`: all eight workspace builds;
- `npm run typecheck`: PASS;
- `npm run lint`: PASS;
- W5 focused service/contract/endpoint/simulation tests: 4 files / 9 tests;
- W5 real-browser journey: 1 passed;
- hidden Unicode check: PASS;
- direct-store boundary: 59 existing approved exceptions, 0 new
  unapproved, 0 stale, 0 duplicate, 0 broad exceptions.

The detached run retained the repository's existing dependency audit limit:
`npm ci` reported 2 low and 7 high advisories. No dependency or security
remediation is claimed by this closure.

## Known limits and non-claims

The following remain explicit limits rather than hidden PASS claims:

- Shanghai evidence is synthetic/assumption-labelled; reality calibration is
  not proven.
- WANT/CAN are candidate/constraint views and never write official truth.
- System Dynamics is shadow-only and does not integrate W4 persistent state.
- JSON internal runtime is the active authority; Postgres/RLS is not active.
- Human Validation A: `NOT_PERFORMED` / historical validation debt.
- Human Model Validation B: `NOT_PERFORMED`; at most `HV-B_READY`.
- No full WCAG/accessibility PASS is claimed.
- Pilot and Production are not authorized and did not occur.
- No automatic successor, release, governance reclosure, or provider/agent
  execution starts from this document.

## Closure disposition

This document is the single W5 Governance Closure candidate. It is
documentation-only and changes no product source, test, contract, workflow,
migration, settlement, runtime, or database state. Its exact governance head
must pass the repository's required checks before ordinary governance merge.

After that merge, an external post-governance readback must bind the final
governance merge SHA and tree to the W5 evidence record. No self-referential
claim is made before that readback. The final status may be
`W5_G5_COMPLETE_WITH_LIMITS` only after the governance merge and external
readback; no later wave is auto-started.
