# Reference Cases

`inputs/` contains ten synthetic, checked-in two-market cases. `expected/` is generated by the explicit freeze command under the pinned Python and PyBLP environment. Inputs and expected outputs are never consumed by the SimWar application runtime.
