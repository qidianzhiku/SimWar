# Third-Party Notices

The POC pins `pyblp==1.2.0` and its numerical dependencies in `requirements.lock`. Reviewers must consult the installed distribution metadata and upstream license texts before any redistribution. This reference tool is isolated, synthetic, and removable; it does not add a SimWar runtime dependency.
